# Conexão com o PostgreSQL — ajuste a senha aqui (ou via variáveis AGENDA_DB_*).
# As variáveis de ambiente, quando definidas, têm prioridade sobre este arquivo.
db.url=jdbc:postgresql://localhost:5432/agenda
db.user=postgres
db.password=postgres
