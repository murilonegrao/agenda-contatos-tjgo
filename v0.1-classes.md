---
title: "v0.1 — Diagrama de Classes (núcleo + console)"
type: project-doc
tags: [projeto, pi2a, design, classes, uml, java, dao, service, agenda-tjgo]
status: v0.1
criado: 2026-06-15
atualizado: 2026-06-15
---

> [!info] Onde isto se encaixa
> Visão de **implementação** do **núcleo Java**. Os pacotes `model` + `dao` + `service` sustentam o console (entrega do prof). Decisões de stack em [ADR-001-stack](ADR-001-stack.md). Hub em [README](README.md).

# Organização em pacotes

| Pacote | Responsabilidade | Usado por |
|--------|------------------|-----------|
| `model` | POJOs de domínio — espelham as tabelas. Sem regra de banco. | todos |
| `dao` | **Data Access Objects** — JDBC puro com `PreparedStatement`. | service |
| `service` | Regras de negócio, orquestração de DAOs, **busca**. Ponto único de lógica. | console + API |
| `app` | `MenuConsole` (a `main`) — interação por texto. **Entrega acadêmica.** | — |

> [!tip] Por que uma camada `service`
> O console e a API REST chamam **a mesma** `service`, então a regra de negócio (validação, arco exclusivo, busca) é escrita **uma vez** e reusada nas duas faces. Sem isso, duplicaríamos lógica entre `MenuConsole` e os controllers da API.

# Diagrama de classes (núcleo + console)

```mermaid
classDiagram
    direction LR

    namespace model {
        class Unidade {
            -int id
            -String nome
            -TipoUnidade tipo
            -String endereco
            -Integer responsavelId
            -LocalDateTime criadoEm
        }
        class Contato {
            -int id
            -String nome
            -String cargo
            -LocalDateTime criadoEm
        }
        class Lotacao {
            -int unidadeId
            -int contatoId
            -LocalDate desde
        }
        class Telefone {
            -int id
            -String numero
            -TipoTelefone tipo
            -Integer unidadeId
            -Integer contatoId
        }
        class Email {
            -int id
            -String endereco
            -String tipo
            -Integer unidadeId
            -Integer contatoId
        }
        class TipoUnidade {
            <<enumeration>>
            UNIDADE_PRISIONAL
            ORGAO
            OUTRO
        }
        class TipoTelefone {
            <<enumeration>>
            CELULAR
            FIXO
            WHATSAPP
            PLANTAO
        }
    }

    namespace dao {
        class ConexaoFactory {
            +getConnection() Connection
        }
        class UnidadeDAO {
            +inserir(Unidade) int
            +buscarPorId(int) Unidade
            +listar() List~Unidade~
            +atualizar(Unidade) void
            +remover(int) void
            +definirResponsavel(int, Integer) void
        }
        class ContatoDAO {
            +inserir(Contato) int
            +buscarPorId(int) Contato
            +listar() List~Contato~
            +atualizar(Contato) void
            +remover(int) void
        }
        class LotacaoDAO {
            +vincular(int, int) void
            +desvincular(int, int) void
            +listarContatosDaUnidade(int) List~Contato~
            +listarUnidadesDoContato(int) List~Unidade~
        }
        class TelefoneDAO {
            +inserir(Telefone) int
            +listarPorUnidade(int) List~Telefone~
            +listarPorContato(int) List~Telefone~
            +remover(int) void
        }
        class EmailDAO {
            +inserir(Email) int
            +listarPorUnidade(int) List~Email~
            +listarPorContato(int) List~Email~
            +remover(int) void
        }
    }

    namespace service {
        class AgendaService {
            +salvarUnidade(Unidade) int
            +salvarContato(Contato) int
            +vincularLotacao(int, int) void
            +desvincularLotacao(int, int) void
            +definirResponsavel(int, Integer) void
            +addTelefone(Telefone) int
            +addEmail(Email) int
            +buscarPorNome(String) List~ResultadoBusca~
        }
    }

    namespace app {
        class MenuConsole {
            +main(String[]) void
        }
    }

    UnidadeDAO  ..> Unidade
    ContatoDAO  ..> Contato
    LotacaoDAO  ..> Lotacao
    TelefoneDAO ..> Telefone
    EmailDAO    ..> Email
    Unidade     --> TipoUnidade
    Telefone    --> TipoTelefone
    AgendaService ..> UnidadeDAO
    AgendaService ..> ContatoDAO
    AgendaService ..> LotacaoDAO
    AgendaService ..> TelefoneDAO
    AgendaService ..> EmailDAO
    UnidadeDAO  ..> ConexaoFactory
    ContatoDAO  ..> ConexaoFactory
    LotacaoDAO  ..> ConexaoFactory
    TelefoneDAO ..> ConexaoFactory
    EmailDAO    ..> ConexaoFactory
    MenuConsole ..> AgendaService
```

# Decisões de design das classes

- **POJOs sem lógica** — `model` só carrega dados. Validação fica no banco (CHECK/FK) + na `service`.
- **Lotação como N:N** — `Contato` não tem mais `unidadeId`; o vínculo vive em `Lotacao` e é gerenciado por `LotacaoDAO` (`vincular`/`desvincular`).
- **FKs como `Integer` (wrapper)** — para representar `NULL` (responsável/dono opcionais). `int` não distingue "0" de "ausente".
- **`PreparedStatement` sempre** — contra SQL injection e para tratamento limpo de `SQLException` (RF08).
- **Tratamento de exceções** — a `service` lança exceções de domínio claras; o `MenuConsole` (e os controllers da API) as traduzem em mensagem amigável / status HTTP.
- **`numero` de telefone** — armazenado/manipulado como dígitos com DDI; formatação só na apresentação.

# Mapeamento classe ↔ tabela

| Classe (`model`) | Tabela | DAO |
|------------------|--------|-----|
| `Unidade` | `unidade` | `UnidadeDAO` |
| `Contato` | `contato` | `ContatoDAO` |
| `Lotacao` | `lotacao` | `LotacaoDAO` |
| `Telefone` | `telefone` | `TelefoneDAO` |
| `Email` | `email` | `EmailDAO` |
